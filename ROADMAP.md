[ ] create settings page
[ ] implement `id` in quote
[ ] fix `search` text field hint color
[ ] `search` should filter and display `any` existent `clients`/`services`/`items` -not only added ones.
[x] page to create `items`
[ ] page to edit `items`
[ ] select emoji for `items` on `create`/`edit` page
[x] page to create `clients`
[ ] page to edit `clients`
[ ] select emoji for `clients` on `create`/`edit` page
[x] page to create `services`
[ ] page to edit `unit`
[ ] easy way of editing `item` price during quote
[ ] easy way of editing `service` price during quote
[ ] add pages should display only added `clients`/`services`/`items` in current draft quote
[ ] order `recent quotes` by latest first
[ ] open `recent quote` by clicking
[ ] openening `saved` quote only shows up `review` & `send` steps
[ ] display pdf
[ ] share pfd
[ ] improve fonts
[ ] improve `add buttons` in `clients`/`services`/`items` page
[ ] remove continue button from home
[ ] translate to portuguese
